#name: Automatic Semantic Release
#
#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:
#
#permissions:
#  contents: write
#  packages: write
#
#jobs:
#  release:
#    name: Create Git Tag & Release
#    runs-on: ubuntu-latest
#
#    steps:
#      # 1️⃣ Checkout code
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0  # required for full tag history
#
#      # 2️⃣ Set up Node.js for semantic-release
#      - name: Set up Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: 20
#
#      # 3️⃣ Install and configure semantic-release
#      - name: Install semantic-release
#        run: |
#          npm install -g semantic-release @semantic-release/changelog @semantic-release/git @semantic-release/github
#
#      # 4️⃣ Run release
#      - name: Run semantic-release
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        run: |
#          npx semantic-release
#
#      # 5️⃣ Summary
#      - name: Summary
#        run: |
#          echo "✅ Automatic semantic release completed!"
#          echo "📦 If version bump detected, a new tag and release were created."
